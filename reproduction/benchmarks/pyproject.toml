[project]
name = "thesis-benchmark-reproduction"
version = "0.1.0"
description = "Generate duration boxplots from exported Theia Cloud session benchmark CSV files."
requires-python = ">=3.13"
dependencies = [
    "matplotlib>=3.10.1",
]

[tool.uv]
package = false
